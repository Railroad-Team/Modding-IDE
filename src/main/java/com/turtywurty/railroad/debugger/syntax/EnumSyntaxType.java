package com.turtywurty.railroad.debugger.syntax;

// Add more. This is very bare
public enum EnumSyntaxType {
	TEXT, KEYWORD, NAME, LITERAL, STRING, NUMBER, OPERATORS, PUNC, COMMENT, ELSE, FUNCTION
}
